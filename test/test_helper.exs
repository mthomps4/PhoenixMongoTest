ExUnit.start()

# Ecto.Adapters.SQL.Sandbox.mode(Lab.Repo, :manual)

# {:ok, files} = File.ls("./test/support")
#
# Enum.each files, fn(file) ->
#   Code.require_file "support/#{file}", __DIR__
# end
